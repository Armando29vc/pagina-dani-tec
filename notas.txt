# Subir cambios git add .
# Colocar comentarios git commit -m "comentario"
# Confirmar cambios  git push origin main
# git commit

//Descargar cambios de github
git pull

//ignorar archivos
git rm --cached js/tema.js